#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Use local prettierignore for pre-push validation
if [ -f .prettierignore.local ]; then
  pnpm exec prettier --check . --ignore-path .prettierignore.local
else
  pnpm run format:check
fi

pnpm run lint && pnpm run typecheck