{
  "name": "@peac/example-action-approval-records",
  "version": "0.0.0",
  "private": true,
  "description": "PEAC example: single-issuer agent-action records for the action-approval flow (approve or deny an action intent; report the invocation on the approved path), verified offline",
  "type": "module",
  "scripts": {
    "demo": "tsx demo.ts",
    "demo:tamper": "tsx demo.ts --tamper",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@peac/crypto": "workspace:*",
    "@peac/protocol": "workspace:*",
    "@peac/schema": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
