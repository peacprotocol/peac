{
  "$comment": "PEAC Protocol MCP server config for Cursor. Pin an exact version; update the pin on each PEAC release. Companion to peac.mdc (the Cursor project rule).",
  "mcpServers": {
    "peac": {
      "command": "npx",
      "args": ["-y", "@peac/mcp-server@0.14.5"],
      "env": {}
    }
  }
}
