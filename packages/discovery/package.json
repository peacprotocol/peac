{
  "name": "@peac/discovery",
  "version": "0.9.12.1",
  "description": "PEAC discovery with ≤20 lines enforcement",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "build:types": "tsc -p tsconfig.types.json",
    "prepublishOnly": "pnpm run build && pnpm run build:types",
    "pretest": "pnpm -w turbo run build --filter=@peac/discovery",
    "test": "tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  },
  "keywords": [
    "peac",
    "discovery",
    "well-known",
    "abnf"
  ],
  "license": "MIT",
  "repository": "https://github.com/peac-protocol/peac",
  "sideEffects": false
}
